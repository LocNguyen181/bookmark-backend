generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Bookmark {
  id          Int      @id @default(autoincrement())
  url         String
  title       String?
  tags        String?
  dateAdded   DateTime @default(now())
  actressName String?
}
